// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) TypeSpec Code Generator.

package com.azure.resourcemanager.billingbenefits.implementation;

import com.azure.core.http.rest.PagedIterable;
import com.azure.core.http.rest.Response;
import com.azure.core.http.rest.SimpleResponse;
import com.azure.core.util.Context;
import com.azure.core.util.logging.ClientLogger;
import com.azure.resourcemanager.billingbenefits.fluent.SavingsPlanModelsClient;
import com.azure.resourcemanager.billingbenefits.fluent.models.SavingsPlanModelInner;
import com.azure.resourcemanager.billingbenefits.fluent.models.SavingsPlanValidateResponseInner;
import com.azure.resourcemanager.billingbenefits.models.SavingsPlanModel;
import com.azure.resourcemanager.billingbenefits.models.SavingsPlanModels;
import com.azure.resourcemanager.billingbenefits.models.SavingsPlanUpdateRequest;
import com.azure.resourcemanager.billingbenefits.models.SavingsPlanUpdateValidateRequest;
import com.azure.resourcemanager.billingbenefits.models.SavingsPlanValidateResponse;

public final class SavingsPlanModelsImpl implements SavingsPlanModels {
    private static final ClientLogger LOGGER = new ClientLogger(SavingsPlanModelsImpl.class);

    private final SavingsPlanModelsClient innerClient;

    private final com.azure.resourcemanager.billingbenefits.BillingBenefitsManager serviceManager;

    public SavingsPlanModelsImpl(SavingsPlanModelsClient innerClient,
        com.azure.resourcemanager.billingbenefits.BillingBenefitsManager serviceManager) {
        this.innerClient = innerClient;
        this.serviceManager = serviceManager;
    }

    public Response<SavingsPlanModel> getWithResponse(String savingsPlanOrderId, String savingsPlanId, String expand,
        Context context) {
        Response<SavingsPlanModelInner> inner
            = this.serviceClient().getWithResponse(savingsPlanOrderId, savingsPlanId, expand, context);
        if (inner != null) {
            return new SimpleResponse<>(inner.getRequest(), inner.getStatusCode(), inner.getHeaders(),
                new SavingsPlanModelImpl(inner.getValue(), this.manager()));
        } else {
            return null;
        }
    }

    public SavingsPlanModel get(String savingsPlanOrderId, String savingsPlanId) {
        SavingsPlanModelInner inner = this.serviceClient().get(savingsPlanOrderId, savingsPlanId);
        if (inner != null) {
            return new SavingsPlanModelImpl(inner, this.manager());
        } else {
            return null;
        }
    }

    public SavingsPlanModel update(String savingsPlanOrderId, String savingsPlanId, SavingsPlanUpdateRequest body) {
        SavingsPlanModelInner inner = this.serviceClient().update(savingsPlanOrderId, savingsPlanId, body);
        if (inner != null) {
            return new SavingsPlanModelImpl(inner, this.manager());
        } else {
            return null;
        }
    }

    public SavingsPlanModel update(String savingsPlanOrderId, String savingsPlanId, SavingsPlanUpdateRequest body,
        Context context) {
        SavingsPlanModelInner inner = this.serviceClient().update(savingsPlanOrderId, savingsPlanId, body, context);
        if (inner != null) {
            return new SavingsPlanModelImpl(inner, this.manager());
        } else {
            return null;
        }
    }

    public PagedIterable<SavingsPlanModel> list(String savingsPlanOrderId) {
        PagedIterable<SavingsPlanModelInner> inner = this.serviceClient().list(savingsPlanOrderId);
        return ResourceManagerUtils.mapPage(inner, inner1 -> new SavingsPlanModelImpl(inner1, this.manager()));
    }

    public PagedIterable<SavingsPlanModel> list(String savingsPlanOrderId, Context context) {
        PagedIterable<SavingsPlanModelInner> inner = this.serviceClient().list(savingsPlanOrderId, context);
        return ResourceManagerUtils.mapPage(inner, inner1 -> new SavingsPlanModelImpl(inner1, this.manager()));
    }

    public Response<SavingsPlanValidateResponse> validateUpdateWithResponse(String savingsPlanOrderId,
        String savingsPlanId, SavingsPlanUpdateValidateRequest body, Context context) {
        Response<SavingsPlanValidateResponseInner> inner
            = this.serviceClient().validateUpdateWithResponse(savingsPlanOrderId, savingsPlanId, body, context);
        if (inner != null) {
            return new SimpleResponse<>(inner.getRequest(), inner.getStatusCode(), inner.getHeaders(),
                new SavingsPlanValidateResponseImpl(inner.getValue(), this.manager()));
        } else {
            return null;
        }
    }

    public SavingsPlanValidateResponse validateUpdate(String savingsPlanOrderId, String savingsPlanId,
        SavingsPlanUpdateValidateRequest body) {
        SavingsPlanValidateResponseInner inner
            = this.serviceClient().validateUpdate(savingsPlanOrderId, savingsPlanId, body);
        if (inner != null) {
            return new SavingsPlanValidateResponseImpl(inner, this.manager());
        } else {
            return null;
        }
    }

    private SavingsPlanModelsClient serviceClient() {
        return this.innerClient;
    }

    private com.azure.resourcemanager.billingbenefits.BillingBenefitsManager manager() {
        return this.serviceManager;
    }
}
