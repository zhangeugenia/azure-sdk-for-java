// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) TypeSpec Code Generator.

package com.azure.resourcemanager.iotoperations.models;

import com.azure.core.annotation.Immutable;
import com.azure.json.JsonReader;
import com.azure.json.JsonSerializable;
import com.azure.json.JsonToken;
import com.azure.json.JsonWriter;
import java.io.IOException;

/**
 * AkriDiscoveryHandlerSchedule properties.
 */
@Immutable
public class AkriDiscoveryHandlerSchedule implements JsonSerializable<AkriDiscoveryHandlerSchedule> {
    /*
     * Schedule type.
     */
    private AkriDiscoveryHandlerScheduleType scheduleType
        = AkriDiscoveryHandlerScheduleType.fromString("AkriDiscoveryHandlerSchedule");

    /**
     * Creates an instance of AkriDiscoveryHandlerSchedule class.
     */
    public AkriDiscoveryHandlerSchedule() {
    }

    /**
     * Get the scheduleType property: Schedule type.
     * 
     * @return the scheduleType value.
     */
    public AkriDiscoveryHandlerScheduleType scheduleType() {
        return this.scheduleType;
    }

    /**
     * Validates the instance.
     * 
     * @throws IllegalArgumentException thrown if the instance is not valid.
     */
    public void validate() {
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public JsonWriter toJson(JsonWriter jsonWriter) throws IOException {
        jsonWriter.writeStartObject();
        jsonWriter.writeStringField("scheduleType", this.scheduleType == null ? null : this.scheduleType.toString());
        return jsonWriter.writeEndObject();
    }

    /**
     * Reads an instance of AkriDiscoveryHandlerSchedule from the JsonReader.
     * 
     * @param jsonReader The JsonReader being read.
     * @return An instance of AkriDiscoveryHandlerSchedule if the JsonReader was pointing to an instance of it, or null
     * if it was pointing to JSON null.
     * @throws IOException If an error occurs while reading the AkriDiscoveryHandlerSchedule.
     */
    public static AkriDiscoveryHandlerSchedule fromJson(JsonReader jsonReader) throws IOException {
        return jsonReader.readObject(reader -> {
            String discriminatorValue = null;
            try (JsonReader readerToUse = reader.bufferObject()) {
                readerToUse.nextToken(); // Prepare for reading
                while (readerToUse.nextToken() != JsonToken.END_OBJECT) {
                    String fieldName = readerToUse.getFieldName();
                    readerToUse.nextToken();
                    if ("scheduleType".equals(fieldName)) {
                        discriminatorValue = readerToUse.getString();
                        break;
                    } else {
                        readerToUse.skipChildren();
                    }
                }
                // Use the discriminator value to determine which subtype should be deserialized.
                if ("Cron".equals(discriminatorValue)) {
                    return AkriDiscoveryHandlerScheduleCron.fromJson(readerToUse.reset());
                } else if ("RunOnce".equals(discriminatorValue)) {
                    return AkriDiscoveryHandlerScheduleRunOnce.fromJson(readerToUse.reset());
                } else if ("Continuous".equals(discriminatorValue)) {
                    return AkriDiscoveryHandlerScheduleContinuous.fromJson(readerToUse.reset());
                } else {
                    return fromJsonKnownDiscriminator(readerToUse.reset());
                }
            }
        });
    }

    static AkriDiscoveryHandlerSchedule fromJsonKnownDiscriminator(JsonReader jsonReader) throws IOException {
        return jsonReader.readObject(reader -> {
            AkriDiscoveryHandlerSchedule deserializedAkriDiscoveryHandlerSchedule = new AkriDiscoveryHandlerSchedule();
            while (reader.nextToken() != JsonToken.END_OBJECT) {
                String fieldName = reader.getFieldName();
                reader.nextToken();

                if ("scheduleType".equals(fieldName)) {
                    deserializedAkriDiscoveryHandlerSchedule.scheduleType
                        = AkriDiscoveryHandlerScheduleType.fromString(reader.getString());
                } else {
                    reader.skipChildren();
                }
            }

            return deserializedAkriDiscoveryHandlerSchedule;
        });
    }
}
